# IT Toolbox

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

Ứng dụng chỉ cho phép kiểm tra hostname/IP công khai sau khi người dùng xác nhận có quyền kiểm tra. Lịch sử lưu tại `data/toolbox.db`.

